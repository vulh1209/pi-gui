{
  "name": "@pi-gui/desktop",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "description": "Codex-style desktop shell for pi",
  "main": "dist-electron/main.js",
  "scripts": {
    "build:deps": "pnpm --filter @pi-gui/session-driver build && pnpm --filter @pi-gui/catalogs build && pnpm --filter @pi-gui/pi-sdk-driver build",
    "dev": "pnpm run build:deps && concurrently -k -r \"pnpm:dev:renderer\" \"pnpm:dev:electron:watch\" \"pnpm:dev:electron:start\"",
    "dev:renderer": "vite",
    "dev:electron:watch": "tsup electron/main.ts electron/preload.ts --format cjs --platform node --target node22 --out-dir dist-electron --external electron --watch",
    "dev:electron:start": "wait-on dist-electron/main.js dist-electron/preload.js tcp:5173 && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .",
    "build": "pnpm run build:deps && pnpm run build:renderer && pnpm run build:electron",
    "build:renderer": "vite build",
    "build:electron": "tsup electron/main.ts electron/preload.ts --format cjs --platform node --target node22 --out-dir dist-electron --external electron",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.electron.json --noEmit",
    "test": "pnpm build && playwright test tests/smoke.spec.ts tests/worktrees-live.spec.ts",
    "demo:readme": "pnpm run build && node --experimental-strip-types scripts/readme-demo.mts",
    "start": "pnpm run build:deps && electron .",
    "test:live:parallel": "pnpm exec playwright test tests/parallel-live.spec.ts",
    "test:live:controls": "pnpm build && pnpm exec playwright test tests/controls-live.spec.ts",
    "test:live:skills-settings": "pnpm build && pnpm exec playwright test tests/skills-settings-live.spec.ts",
    "test:live:worktrees": "pnpm build && pnpm exec playwright test tests/worktrees-live.spec.ts",
    "test:live:persistence": "pnpm build && pnpm exec playwright test tests/persistence-live.spec.ts",
    "test:live:workspace-menu": "pnpm build && pnpm exec playwright test tests/workspace-menu-live.spec.ts",
    "test:live:notifications": "pnpm build && pnpm exec playwright test tests/notifications-live.spec.ts"
  },
  "dependencies": {
    "@pi-gui/pi-sdk-driver": "workspace:*",
    "@pi-gui/session-driver": "workspace:*",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "electron": "^34.3.0",
    "tsup": "^8.3.5",
    "typescript": "^5.8.2",
    "vite": "^6.2.0",
    "wait-on": "^8.0.2"
  }
}
